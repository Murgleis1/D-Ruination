# Checkpoints live here (gzipped save states, ~42 KB each).
# A checkpoint is tied to the ROM it was made from - after a rebuild that
# shifts code/data layout, re-drive and re-checkpoint if a restore looks wrong.
